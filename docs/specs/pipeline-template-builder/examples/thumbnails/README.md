# Thumbnails (Placeholders)

This folder references thumbnail image paths for starter templates. Replace the placeholder PNGs with actual images when available:

- qc_pack.png — visual snapshot of QC nodes in sequence
- creator_pack.png — snapshot of creator pack graph with ASR/LLM/Gen nodes
- education_pack.png — lecture → chapters → study pack pipeline
- legal_pack.png — ASR+OCR → privilege → redact → Bates → export
- live_pack.png — live ingest → detection → clip → SSAI publish

